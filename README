CONTROLS:

Q-E : rotation (on the Y axis).
A-D : rotation (on the Z axis).
W-S : rotation (on the X axis).

U-O : dezoom/zoom.
I-K : move up/down.
J-L : move left/right.

TAB : change color mode.

1-2 : change color square.
3-4 : change color line.

ESC : quit.
